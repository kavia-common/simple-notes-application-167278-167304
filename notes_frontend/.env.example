# PUBLIC INTERFACE NOTE:
# The frontend expects a REST API for notes. Provide the base URL via:
# VITE_API_BASE_URL: Base URL for the notes REST API (e.g., http://localhost:8000)
# Optionally, you can leave it unset to use the built-in mock API during development.

# PUBLIC_INTERFACE
# VITE_API_BASE_URL=http://localhost:8000
