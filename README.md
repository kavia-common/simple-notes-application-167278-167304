# simple-notes-application-167278-167304

This workspace contains the notes_frontend Astro app for a simple notes application (Ocean Professional theme).

Quickstart
- cd notes_frontend
- npm install
- npm run dev

Optional: create .env and set VITE_API_BASE_URL to point to your notes_database REST API. If unset, a mock in-memory API is used.